# Имплементационен план — Сесија 10 (28.03.2026)

## Цел
Издигнување на апликацијата на **самиот врв** — педагошка интеграција, споделување и квалитет на светско ниво.

---

## П1 — Gamma директно од наставна програма / концепт ✅ → ⬜
**Моментална состојба:** Gamma постои, но само преку AssistantView или MaterialsGenerator. Нема директна врска концепт/тема → Gamma.

**Цел:** Копче „Направи Gamma презентација" во:
- `ConceptDetailView.tsx` — за конкретен поим
- `TopicView.tsx` — за цела тема (наставна единица)

**Имплементација:**
- Вчитај концепт/тема + грејд
- Повикај `generatePresentation(topic, gradeLevel, concepts[])`
- Отвори `GammaModeModal` директно

---

## П3 — Модел на лиценца: Free (јавно) / PRO (приватно) ✅ → ⬜
**Моментална состојба:** Сите зачувани материјали се слободно јавни. Нема разлика.

**Цел:**
- Бесплатна лиценца → материјалот **автоматски јавен** во националната библиотека
- PRO лиценца → наставникот **избира**: приватно (само за себе) или јавно во библиотеката

**Имплементација:**
- `isPublic: boolean` поле на зачуван материјал (Firestore `cached_ai_materials`)
- UI toggle при зачувување: „Сподели во библиотека" / „Чувај приватно (PRO)"
- Firestore query filter: покажи само `isPublic: true` во националната библиотека
- PRO guard за приватно чување

---

## П2 — DataViz График во квиз прашање ✅ → ⬜
**Моментална состојба:** Квиз прашања имаат `svgDiagram` и `imageUrl`, но нема вграден chart.

**Цел:** Наставникот може да додаде DataViz графиk директно во прашање:
- `chartData` + `chartConfig` полиња на `Question` тип
- „Додади Графиk" копче во `GeneratedAssessment.tsx` editor
- `ChartPreview` рендерира во `InteractiveQuizPlayer.tsx` ако постои `chartData`

---

## П4 — Upload слика од наставник во квиз прашање ✅ → ⬜
**Моментална состојба:** Само AI-генерирани слики (`imageUrl` преку Gemini). Нема upload.

**Цел:**
- File input во `GeneratedAssessment.tsx` question editor
- Upload во Firebase Storage → `gs://[bucket]/quiz-images/[uid]/[questionId].jpg`
- URL се зачувува во `imageUrl` полето на прашањето

---

## П5 — Академија: напредок видлив во TeacherProfileView ✅ → ⬜
**Моментална состојба:** `AcademyView` постои со сите функции, но профилот не ги прикажува.

**Цел:**
- `TeacherProfileView.tsx` → нова секција „Академија за наставници"
- Прикажи: XP, streak, завршени модули, сертификати
- Копче „Продолжи со учење →" кон AcademyView

---

## П6 — Gemini Grounding (интернет претрага за реални податоци) ✅ → СЛЕДНА СЕСИЈА
**Комплексно — оставаме за Сесија 11**

---

## Верификациски протокол по секој чекор
1. `npx tsc --noEmit` → 0 грешки
2. `npx vitest run` → 380/380 ✅
3. Рачна UI проверка (логика, edge cases)

---

## Статус
```
П1 Gamma od programa   ⬜ ВО РЕД
П3 Licenca Free/PRO    ⬜ ВО РЕД
П2 Chart vo kviz       ⬜ ВО РЕД
П4 Upload slika        ⬜ ВО РЕД
П5 Akademija Profil    ⬜ ВО РЕД
П6 Grounding           ⬜ СЛЕДНА СЕСИЈА
```

---

*Секој чекор задолжително поминува TSC + тестови пред merge.*
*Апликацијата претендира на самиот врв — нулта толеранција за регресии.*
